# ЮНМ — Готова система за один вечір

Односторінковий лендинг реєстрації на безкоштовний вебінар про банерну монетизацію
YouTube Shorts та Reels (22 вересня, 19:00 за київським часом).

## Що всередині

- `index.html` — вся сторінка: розмітка, стилі та таймер зворотного відліку в одному файлі.
- `cover.webp` — ключовий візуал угорі сторінки, він же прев'ю для соцмереж.

Збірка не потрібна: це статика. Локально достатньо відкрити `index.html` у браузері.

## Що треба оновити перед запуском

1. **Посилання реєстрації.** У `index.html` знайди коментар
   `<!-- Встав сюди посилання на реєстрацію -->` і заміни `href="#"` на реальний URL.
2. **Дата вебінару в таймері.** Внизу `index.html`, у скрипті:
   `new Date('2026-09-22T19:00:00+03:00')`.
3. **og:image.** Після підключення домену заміни `cover.webp` у мета-тезі
   `og:image` на абсолютний URL — інакше прев'ю в Telegram може не підтягнутись.

## Деплой

Статичний хостинг без налаштувань: Vercel імпортує репозиторій як **Other**,
build command порожній, output directory — корінь репозиторію.
